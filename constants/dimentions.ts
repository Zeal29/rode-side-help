import { Dimensions } from "react-native";

export const SCREEN_WIDTH = Dimensions.get("window").width; //full width
export const SCREEN_HEIGHT = Dimensions.get("window").height; //full height

export const PADDING = 32;
export const GAP = 32;

export const CONTAINER_WIDTH = Dimensions.get("window").width - PADDING * 2; //full width
